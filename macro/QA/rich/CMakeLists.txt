GENERATE_ROOT_TEST_SCRIPT(${PANDAROOT_SOURCE_DIR}/macro/qa/rich/sim_complete.C)
add_test(sim_rich ${PANDAROOT_BINARY_DIR}/macro/qa/rich/sim_complete.sh)
SET_TESTS_PROPERTIES(sim_rich PROPERTIES TIMEOUT "400")
SET_TESTS_PROPERTIES(sim_rich PROPERTIES PASS_REGULAR_EXPRESSION "TestPassed;All ok")

GENERATE_ROOT_TEST_SCRIPT(${PANDAROOT_SOURCE_DIR}/macro/qa/rich/digi_complete.C)
add_test(digi_rich ${PANDAROOT_BINARY_DIR}/macro/qa/rich/digi_complete.sh)
SET_TESTS_PROPERTIES(digi_rich PROPERTIES TIMEOUT "200")
SET_TESTS_PROPERTIES(digi_rich PROPERTIES DEPENDS sim_rich)
SET_TESTS_PROPERTIES(digi_rich PROPERTIES PASS_REGULAR_EXPRESSION "TestPassed;All ok")

GENERATE_ROOT_TEST_SCRIPT(${PANDAROOT_SOURCE_DIR}/macro/qa/rich/reco_complete.C)
add_test(reco_rich ${PANDAROOT_BINARY_DIR}/macro/qa/rich/reco_complete.sh)
SET_TESTS_PROPERTIES(reco_rich PROPERTIES TIMEOUT "200")
SET_TESTS_PROPERTIES(reco_rich PROPERTIES DEPENDS digi_rich)
SET_TESTS_PROPERTIES(reco_rich PROPERTIES PASS_REGULAR_EXPRESSION "TestPassed;All ok")

Generate_ROOT_TEST_SCRIPT(${PANDAROOT_SOURCE_DIR}/macro/qa/rich/pid_complete.C)
add_test(pid_rich ${PANDAROOT_BINARY_DIR}/macro/qa/rich/pid_complete.sh)
SET_TESTS_PROPERTIES(pid_rich PROPERTIES TIMEOUT "200")
SET_TESTS_PROPERTIES(pid_rich PROPERTIES DEPENDS reco_rich)
SET_TESTS_PROPERTIES(pid_rich PROPERTIES PASS_REGULAR_EXPRESSION "TestPassed;All ok")
