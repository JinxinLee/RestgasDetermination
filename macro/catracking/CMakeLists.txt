GENERATE_ROOT_TEST_SCRIPT(${PANDAROOT_SOURCE_DIR}/macro/catracking/sim_complete.C)
add_test(sim_ca ${PANDAROOT_BINARY_DIR}/macro/catracking/sim_complete.sh)
SET_TESTS_PROPERTIES(sim_ca PROPERTIES TIMEOUT "400")
SET_TESTS_PROPERTIES(sim_ca PROPERTIES PASS_REGULAR_EXPRESSION "TestPassed;All ok")

GENERATE_ROOT_TEST_SCRIPT(${PANDAROOT_SOURCE_DIR}/macro/catracking/digi_complete.C)
add_test(digi_ca ${PANDAROOT_BINARY_DIR}/macro/catracking/digi_complete.sh)
SET_TESTS_PROPERTIES(digi_ca PROPERTIES TIMEOUT "200")
SET_TESTS_PROPERTIES(digi_ca PROPERTIES DEPENDS sim_ca)
SET_TESTS_PROPERTIES(digi_ca PROPERTIES PASS_REGULAR_EXPRESSION "TestPassed;All ok")

GENERATE_ROOT_TEST_SCRIPT(${PANDAROOT_SOURCE_DIR}/macro/catracking/reco_completeCA.C)
add_test(reco_ca ${PANDAROOT_BINARY_DIR}/macro/catracking/reco_completeCA.sh)
SET_TESTS_PROPERTIES(reco_ca PROPERTIES TIMEOUT "200")
SET_TESTS_PROPERTIES(reco_ca PROPERTIES DEPENDS digi_ca)
SET_TESTS_PROPERTIES(reco_ca PROPERTIES PASS_REGULAR_EXPRESSION "TestPassed;All ok")
