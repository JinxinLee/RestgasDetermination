#include "Panda/Panda.hh"
/**
 *  Test program for BbrIOStreamUtils.
 *
 *  Currently tests only the restoration of ios::basefield.
 *
 *  $Id: testIOStreamUtils.cc,v 1.1.1.1 2005/03/29 16:49:16 steinke Exp $
 */

#include <iostream>

#include "BbrStdUtils/BbrIOStreamUtils.hh"
using std::cout;
using std::dec;
using std::endl;
using std::hex;
using std::ios;

int 
main( int, char** )
{
  cout << "This is thirty-one in hex:          " << hex << 31 << ".\n";
  cout << "This is thirty-one in decimal:      " << dec << 31 << ".\n";
  {
    babar::IOStream::all_format_guard guard( cout );
    cout << "This is thirty-one in hex again:    " << hex << 31 
	 << ", inside a block with an all_format_guard.\n";
  }
  cout << "This is thirty-one after the block: " << dec << 31 << "." << endl;

  return 0;
}
